name: Competitor benchmark history

on:
  schedule:
    - cron: "17 3 * * 1"
  workflow_dispatch:
    inputs:
      messages:
        description: Number of messages per single-node benchmark scenario
        required: false
        default: "10000"
      cluster_comparison_messages:
        description: Number of messages per replicated competitor publish scenario
        required: false
        default: "10000"
      repetitions:
        description: Independent repetitions to aggregate per benchmark suite
        required: false
        default: "3"

permissions:
  contents: read

concurrency:
  group: benchmark-history
  cancel-in-progress: false

jobs:
  benchmark:
    name: Run competitor comparisons
    runs-on: ubuntu-24.04
    timeout-minutes: 40
    env:
      BENCHMARK_PROFILE: ${{ github.event_name == 'schedule' && 'scheduled' || 'manual' }}
      BENCHMARK_MESSAGES: ${{ inputs.messages || '10000' }}
      BENCHMARK_CLUSTER_COMPARISON_MESSAGES: ${{ inputs.cluster_comparison_messages || '10000' }}
      BENCHMARK_REPETITIONS: ${{ inputs.repetitions || '3' }}
    steps:
      - uses: actions/checkout@v7.0.1
      - uses: dtolnay/rust-toolchain@1.97.1
      - uses: Swatinem/rust-cache@v2.9.2
      - name: Run native competitor repetitions
        run: |
          set -euo pipefail
          mkdir -p benchmark-results/raw benchmark-results/normalized
          for repetition in $(seq 1 "$BENCHMARK_REPETITIONS"); do
            python3 scripts/benchmarks2/compare.py \
              --backends kafka,redpanda,nats \
              --messages "$BENCHMARK_MESSAGES" \
              --payload-sizes 100,1024 \
              --cpus 2 \
              --memory 2g \
              --client-cpus 1 \
              --client-memory 512m \
              --output "benchmark-results/raw/native-${repetition}.json"
            python3 scripts/benchmarks2/normalize.py \
              --input "benchmark-results/raw/native-${repetition}.json" \
              --output "benchmark-results/normalized/native-repetition-${repetition}.json"
          done
          python3 scripts/benchmarks2/aggregate.py \
            --inputs benchmark-results/normalized/native-repetition-*.json \
            --output benchmark-results/normalized/native-aggregate.json
      - name: Run three-node competitor repetitions
        run: |
          set -euo pipefail
          mkdir -p benchmark-results/raw benchmark-results/normalized
          for repetition in $(seq 1 "$BENCHMARK_REPETITIONS"); do
            python3 scripts/benchmarks2/compare.py \
              --nodes 3 \
              --backends kafka,redpanda,nats \
              --messages "$BENCHMARK_CLUSTER_COMPARISON_MESSAGES" \
              --payload-sizes 100,1024 \
              --cpus 2 \
              --memory 2g \
              --client-cpus 1 \
              --client-memory 512m \
              --output "benchmark-results/raw/cluster-comparison-${repetition}.json"
            python3 scripts/benchmarks2/normalize.py \
              --input "benchmark-results/raw/cluster-comparison-${repetition}.json" \
              --output "benchmark-results/normalized/cluster-comparison-repetition-${repetition}.json"
          done
          python3 scripts/benchmarks2/aggregate.py \
            --inputs benchmark-results/normalized/cluster-comparison-repetition-*.json \
            --output benchmark-results/normalized/cluster-comparison-aggregate.json
      - name: Upload raw and aggregated results
        uses: actions/upload-artifact@v7.0.1
        with:
          name: competitor-benchmark-run-${{ github.run_id }}
          path: benchmark-results
          if-no-files-found: error

  publish:
    name: Update benchmark history
    needs: benchmark
    if: needs.benchmark.result == 'success'
    runs-on: ubuntu-24.04
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v7.0.1
        with:
          fetch-depth: 0
      - name: Download benchmark result
        uses: actions/download-artifact@v8.0.1
        with:
          name: competitor-benchmark-run-${{ github.run_id }}
          path: benchmark-results
      - name: Append benchmark history
        env:
          GITHUB_SHA: ${{ github.sha }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          GITHUB_RUN_ID: ${{ github.run_id }}
          GITHUB_SERVER_URL: ${{ github.server_url }}
        run: |
          set -euo pipefail
          git fetch origin benchmark-history || true
          if git show-ref --quiet refs/remotes/origin/benchmark-history; then
            git worktree add --detach history origin/benchmark-history
          else
            git worktree add --detach history
            git -C history switch --orphan benchmark-history
            git -C history rm -rf . >/dev/null 2>&1 || true
            git -C history clean -fdx
          fi
          mkdir -p history/runs
          short_sha="${GITHUB_SHA:0:12}"
          for result in benchmark-results/normalized/*-aggregate.json; do
            run_timestamp=$(python3 -c 'import json, sys; print(json.load(open(sys.argv[1], encoding="utf-8"))["generated_at"].replace(":", "").replace("+00:00", "Z"))' "$result")
            suite=$(basename "$result" -aggregate.json)
            cp "$result" "history/runs/${run_timestamp}-${short_sha}-${suite}.json"
          done
          python3 scripts/benchmarks2/build_history.py \
            --runs history/runs \
            --output history/site
          git -C history add runs site
          git -C history \
            -c user.name="github-actions[bot]" \
            -c user.email="41898282+github-actions[bot]@users.noreply.github.com" \
            commit -m "Record benchmark run ${GITHUB_SHA}" || true
          git -C history push origin HEAD:benchmark-history
